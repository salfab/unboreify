{
  "default": {
    "require": ["cypress/support/step_definitions/**/*.ts"],
    "requireModule": ["ts-node/register"],
    "format": ["progress"],
    "formatOptions": {
      "snippetInterface": "async-await"
    },
    "parallel": 1,
    "retry": 0,
    "strict": false,
    "dryRun": false
  }
}
